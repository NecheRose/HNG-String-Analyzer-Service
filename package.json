{
  "name": "hng-string-analyzer-service",
  "version": "1.0.0",
  "description": "String Analyzer Service is a RESTful API that analyzes strings and stores their computed properties for retrieval and filtering. It supports analytical computations such as **palindrome detection**, **word counting**, **unique character tracking**, and **SHA-256 hashing for string identification**.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js",
    "start": "node server.js"
  },
  "keywords": [],
  "type": "module",
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "mongodb": "^6.20.0",
    "mongoose": "^8.19.2"
  }
}
